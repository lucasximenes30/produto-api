spring:
  application:
    name: getma
  datasource:
    url: jdbc:h2:mem:produtos
    username: sa
    password: teste
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /banco
